# CIFAR-100 / ResNet-20 — cs-giung baseline. Smaller secondary model
# for fast iteration / param-sensitivity tables.
include: ../_base.yaml

model: resnet20
batch_size: 128
epochs: 200
warmup_epochs: 5
lr: 0.1
weight_decay: 5.0e-4
